interface:
  display_name: "jimeng-image-gen-opencli"
  short_description: "使用 skill 私有 OpenCLI runtime 稳定驱动即梦生图，并下载 4 张结果图到本地"
  default_prompt: "Use $jimeng-image-gen-opencli to prepare the skill-managed OpenCLI runtime, verify the browser extension and Dreamina login, generate Dreamina images with model and aspect controls, and download all 4 results locally."

policy:
  allow_implicit_invocation: true
